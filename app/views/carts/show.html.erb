
<p id="notice"><%= notice %></p>

<h1>Carts</h1>

<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Price</th>
    <th>Account_id</th>
  </tr>
  </thead>

  <tbody>
  <% @products.each do |product| %>

    <tr>
      <td><%= product.name %></td>
      <td><%= product.description %></td>
      <td><%= product.price %></td>
      <td><%= product.cart_id %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

